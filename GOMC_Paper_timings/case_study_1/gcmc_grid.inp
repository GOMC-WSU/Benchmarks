#PBS -q accq
#PBS -l select=1:ncpus=CCC:mem=8gb:cpu_speed=2.4
#PBS -j oe
#PBS -m abe
#PBS -M fe1855@wayne.edu

echo Running on host `hostname`
echo Time is `date`

cd RUN_DIR
echo Directory is `pwd`

module load intel-16/compiler

# Run job

./GOMC_CPU_GCMC +pCCC in.conf > out.log
